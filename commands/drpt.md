---
description: daily-report 的短别名，等价于 /daily-report:daily-report
argument-hint: [--d YYYY-MM-DD] [--fs <<CALENDAR_PLATFORM>文档URL>]
allowed-tools: [Read, Glob, Grep, Bash, Write, Edit]
---

# drpt — 日报短别名

`drpt` 是 `daily-report` 的短别名。

执行本命令前，完整读取 `../skills/daily-report/SKILL.md` 并严格遵循该流程。用户参数：`$ARGUMENTS`
